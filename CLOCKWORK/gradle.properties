org.gradle.jvmargs = -Xmx4G
org.gradle.daemon = false
org.gradle.parallel = true
org.gradle.caching = true

# Identity
mod_name = clockwork
mod_id = vs_clockwork
mod_version = 0.5.6
archives_base_name = clockwork
maven_group = org.valkyrienskies

# Target: Fabric only. Forge is dropped -- 1.21.11 is NeoForge territory and the
# Valkyrien Skies builds that reach this version are Fabric.
minecraft_version = 1.21.11
loader_version = 0.19.4
loom_version = 1.16.3
fabric_version = 0.141.6+1.21.11

# Kotlin
kotlin_version = 2.0.0
fabric_language_kotlin_version = 1.11.0+kotlin.2.0.0

# UNVERIFIED -- this environment blocks the mod mavens, so these coordinates were
# not resolved against a repository. Confirm each against the linked source before
# the first build; see PORTING.md "Dependencies to pin".
#
# Valkyrien Skies has no official 1.21.11 release. The community ports do:
#   https://modrinth.com/project/GE25t0ct  (VS [UNOFFICIAL] Port, Fabric 1.21.11)
vs2_version = 3.1.1
# Kelvin ships with VS; match whatever the VS build above depends on.
kelvin_version =
# https://modrinth.com/mod/create-fabric/versions -> 1.21.11
create_fabric_version = 6.0.9

curse_project_id = 807792
modrinth_project_id = 84USeAvk
